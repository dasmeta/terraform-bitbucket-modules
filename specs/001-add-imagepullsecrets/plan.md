# Implementation Plan: Add imagePullSecrets Support

**Branch**: `001-add-imagepullsecrets` | **Date**: 2026-05-18 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/001-add-imagepullsecrets/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/plan-template.md` for the execution workflow.

## Summary

Add optional `image_pull_secrets` and `namespace_labels` inputs to the root Bitbucket module wrapper and runner Terraform submodule. Image pull secrets pass through to the Kubernetes CronJob pod template as pod-level image pull secrets; namespace labels pass through to the created namespace metadata. The change is backward-compatible through empty defaults. Update root/runner docs and runner test/example coverage.

## Technical Context

<!--
  ACTION REQUIRED: Replace the content in this section with the technical details
  for the project. The structure here is presented in advisory capacity to guide
  the iteration process.
-->

**Language/Version**: Terraform, repository requires Terraform `>= 1.3.0` in runner tests  
**Primary Dependencies**: HashiCorp Kubernetes provider `2.23.0`, Terraform built-in test provider  
**Storage**: N/A  
**Testing**: Existing Terraform test-style files under `modules/runner/tests/basic/`; validation with `terraform fmt` and `terraform init/validate` where provider access allows  
**Target Platform**: Kubernetes CronJob workload generated by Terraform module
**Project Type**: Terraform module repository  
**Performance Goals**: N/A; configuration-only module change  
**Constraints**: Must be backward-compatible with existing consumers; must stay scoped to root wrapper pass-through and `modules/runner`; must not affect `modules/deployments`  
**Scale/Scope**: Root wrapper inputs, runner submodule inputs, one CronJob pod-spec rendering change, one namespace metadata labels change, root/runner README updates, runner test/example updates

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

Project constitution file is still the generated placeholder and has no concrete enforceable gates. Jira/Terraform-module guidance applies:

- Preserve existing module interface behavior for consumers that omit the new input.
- Keep the wrapper interface narrow and opinionated; expose only image pull secret names.
- Update documentation and tests/examples alongside the module behavior change.
- Keep scope limited to root wrapper pass-through, `modules/runner`, and Speckit artifacts.

## Project Structure

### Documentation (this feature)

```text
specs/[###-feature]/
├── plan.md              # This file (/speckit.plan command output)
├── research.md          # Phase 0 output (/speckit.plan command)
├── data-model.md        # Phase 1 output (/speckit.plan command)
├── quickstart.md        # Phase 1 output (/speckit.plan command)
├── contracts/           # Phase 1 output (/speckit.plan command)
└── tasks.md             # Phase 2 output (/speckit.tasks command - NOT created by /speckit.plan)
```

### Source Code (repository root)
<!--
  ACTION REQUIRED: Replace the placeholder tree below with the concrete layout
  for this feature. Delete unused options and expand the chosen structure with
  real paths (e.g., apps/admin, packages/something). The delivered plan must
  not include Option labels.
-->

```text
modules/runner/
├── variables.tf
├── cronjob.tf
├── namespace.tf
├── README.md
└── tests/basic/
    ├── 0-setup.tf
    ├── 1-example.tf
    └── 2-assert.tf

main.tf
variables.tf
README.md
```

**Structure Decision**: Implement behavior in `modules/runner`, expose pass-through input at the repository root wrapper, and update both root and runner documentation. Do not change unrelated deployment module files.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| [e.g., 4th project] | [current need] | [why 3 projects insufficient] |
| [e.g., Repository pattern] | [specific problem] | [why direct DB access insufficient] |
